<?xml version="1.0"?>
<project name="server" default="jar" basedir=".">
  <target name="clean">
    <delete dir="${basedir}/build" />
  </target>

  <target name="compile">
    <mkdir dir="${basedir}/build/classes" />
    <javac destdir="${basedir}/build/classes" debug="true" encoding="utf8">
      <src path="${basedir}/src" />
      <include name="**/*.java" />
    </javac>
    <copy todir="${basedir}/build/classes">
      <fileset dir="${basedir}/src">
        <include name="**/*.properties"/>
      </fileset>
    </copy>
  </target>

  <target name="jar" depends="compile">
    <delete file="${basedir}/build/transform.jar" />
    <jar destfile="${basedir}/build/transform.jar">
      <fileset dir="${basedir}/build/classes"
               includes="**/*.class" />
    </jar>
  </target>
</project>
